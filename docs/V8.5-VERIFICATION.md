# V8.5 Verification — Production Trust + Auth + Privacy + Multi-User + Observability

Every claim below comes from a command actually executed against this working
tree (Linux, Python 3.13.14, Node v20.20.2 / npm 10.8.2). Nothing is simulated.

## Full backend regression + V8.5 suites

- `cd backend && pytest` — **858 passed, 22 skipped, exit 0** in one run.
  The 22 skips are the repository's pre-existing explicit
  environment-dependent Ollama/real-model skips; none were added by V8.5.
- Every pre-V8.5 module (V8.1 → V8.4.4) passes unchanged with
  `AUTH_MODE=disabled` defaults — V8.4.1/2/3/4 behavior preserved.

## V8.5 security suites (subset of the run above)

| Suite | Result | Covers |
|---|---|---|
| `test_v85_auth.py` | **11 passed** | hashing round-trip/no plaintext, register→login→session→logout, uniform invalid-credential 401s (no account oracle), unauthenticated 401s, server-side expiry, revocation, fixation (fresh token per login, independent revocation), hashed-only token storage, HttpOnly/SameSite cookie flags, disabled-account lockout + session kill, registration validation |
| `test_v85_authorization.py` | **7 passed** | least-privilege role model, member→admin surface 403s with audited denials, admin→owner escalation blocked, role change audited, cross-tenant admin actions → 404, mass assignment → 422, self-disable/self-demote refused |
| `test_v85_isolation.py` | **16 passed** | memories/search/events/turn-replay/execution/conversations/world/exports/restore-history/research/explanations/skills/principles/decisions/missions all owner-scoped; direct-id IDOR → 404; query+body `user_id` substitution ignored; planted data lands in the caller's own namespace; foreign export package rejected at validation AND restore; thread-id collision refused (409) and checkpoint reads gated; cognitive tool schemas expose no user/tenant parameter; direct DB owner-scope sweep |
| `test_v85_security.py` | **14 passed** | auth/API/portability rate limits (429, Retry-After, per-principal isolation, metric + `security.rate_limited` audit), CSRF enforcement (missing/wrong header → 403, correct header passes, bearer exempt, GET exempt), oversized body → 413, malformed JSON → 422 with no traceback, 404s free of stack traces/file paths and carrying request ids, login responses free of password material, redaction filter, repository-wide static secret scan, `.env.example` placeholder audit, PRODUCTION gate (unsafe config → RuntimeError at startup; safe config validates clean) |
| `test_v85_audit_observability.py` | **12 passed** | SECURITY_V85 types are canonical EventBus vocabulary; auth lifecycle audited in `cognitive_events` (no parallel store); audit payloads free of passwords/tokens/hashes with redacted emails; X-Request-ID generated, echoed, honoured when supplied, and present in error bodies; metrics count requests/auth failures with route-template labels and zero private content; `/api/health/live` is liveness-only; `/api/health/ready` reports per-dependency `ACTIVE/DEGRADED/NOT_CONFIGURED/BLOCKED/FAILED` with the demo provider honestly NOT_CONFIGURED; admin security-event review; observable rate-limit state |
| `test_v85_migration.py` | **5 passed** | real single-user DB built through the disabled-mode V8.4.4 path reopens under `AUTH_MODE=required` with every row-count identical; owner adopts the legacy namespace and sees legacy memories/world/missions/pre-upgrade exports; adoption idempotent; second-account theft → 409; schema strictly additive |

## Frontend

- `npm run typecheck` — clean.
- `npm run lint` — `✔ No ESLint warnings or errors`.
- `npm run build` — 6/6 static pages including `/observatory` with the new
  `SecurityPanel` and `SystemHealthPanel`.

## Browser QA (`tests/v85_browser_qa.py`) — 16/16 PASS

Against the production build (`next start`) proxying to a live backend in
local mode, plus a second live backend on port 8001 with
`AUTH_MODE=required`:

1. Security panel renders. 2. Honest local-mode state ("No authentication is
enforced" / NOT CONFIGURED — never a fake logged-in user). 3. System health
panel renders. 4. Dependency truth vocabulary displayed. 5. Database
dependency shown ACTIVE from a real probe. 6. No `pbkdf2` strings in the DOM.
7. No bearer tokens in the DOM. 8. Mobile 390 px: 0 px horizontal overflow.
9. Zero console errors (also re-checked across `/`, `/memory`,
`/architecture`, `/workspace`). 10. required mode: anonymous `/api/memories`
→ 401. 11. registration works. 12. login works. 13. login response contains
no password material. 14. `/api/auth/session` resolves the principal.
15. logout succeeds. 16. the token is dead after logout.

## Clean room

Fresh extraction of the release tree into an empty directory, fresh
`python3 -m venv`, `pip install -r backend/requirements.txt`, then:

- backend: `pytest` — **858 passed, 22 skipped, exit 0**;
- frontend: fresh `npm ci` (0 vulnerabilities), `typecheck` clean, `lint`
  clean, production `build` 6/6 pages.

## Release package

`memory-os-showcase-V8.5-PRODUCTION-TRUST-AUTH-PRIVACY-FINAL.zip` built by
`scripts/make_release_zip.py`: excludes `.git`, virtualenvs, `node_modules`,
runtime databases, caches, logs, key/cert files and env files
(`.env.example` retained); CRC verified `OK`; SHA-256 recorded in the
adjacent `.sha256` file and re-verified after writing.

## Honest notes

- The 22 skipped tests require a live Ollama server; they skip with explicit
  reasons, exactly as in V8.4.x releases.
- Rate limiting and metrics are single-process by design and say so in their
  own reported state.
- No login page exists in the demo frontend; required-mode auth flows were
  verified through the API and the Observatory session surface.
